# Frontend / Web dependencies & build output
node_modules/
dist/
.vite/

# Python artifacts & environment
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
*.egg-info/
.venv/
venv/
env/

# Binary models & runtime data directories.
# Root-anchored: a bare "memory/" would also match the source package at
# hermes-agent/skills/voice_avatar/memory/. These match only the runtime
# dirs created at the repo root (see MODEL_CACHE_DIR / AGENT_KEYSTORE_DIR /
# PERSISTENT_MEMORY_DIR in .env.example).
*.onnx
/models/
/keys/
/memory/

# Environment configuration
.env
.env.local
